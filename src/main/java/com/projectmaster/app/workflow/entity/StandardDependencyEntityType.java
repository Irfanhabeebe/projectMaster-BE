package com.projectmaster.app.workflow.entity;

/**
 * Enum representing the types of entities that can have dependencies in standard workflows
 */
public enum StandardDependencyEntityType {
    STAGE,  // Standard workflow stages
    TASK,   // Standard workflow tasks  
    STEP    // Standard workflow steps
}
